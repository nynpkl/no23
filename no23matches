import React, { useMemo, useState } from "react";
import { Search, CalendarDays, BarChart3, PlayCircle, Lock, Users, Trophy, ExternalLink, Filter } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

const matches = [
  {
    id: 1,
    date: "2026-05-01",
    category: "U10",
    league: "TBF Seri C - G Grubu",
    home: "No23 Basketball Academy",
    away: "Fatih Belediyesi SK",
    venue: "BGM Salon C3",
    score: "",
    videoUrl: "https://youtube.com/",
    statsUrl: "https://docs.google.com/",
    tags: ["Lig Maçı", "Tam Maç"],
    note: "Maç videosu ve istatistik linki eklendi."
  },
  {
    id: 2,
    date: "2026-04-26",
    category: "U9",
    league: "Unibest Ligi",
    home: "No23 Basketball Academy",
    away: "Anka Avrupa",
    venue: "İstanbul",
    score: "",
    videoUrl: "https://youtube.com/",
    statsUrl: "https://docs.google.com/",
    tags: ["Gelişim", "Ata #0"],
    note: "Ata için bireysel gelişim takibi yapılabilir."
  },
  {
    id: 3,
    date: "2026-04-20",
    category: "U8",
    league: "Hazırlık / Gelişim",
    home: "No23 Basketball Academy",
    away: "Petna",
    venue: "İstanbul",
    score: "",
    videoUrl: "https://youtube.com/",
    statsUrl: "https://docs.google.com/",
    tags: ["Tam Maç"],
    note: "Örnek maç kartı."
  }
];

function formatDate(dateString) {
  return new Intl.DateTimeFormat("tr-TR", {
    day: "2-digit",
    month: "long",
    year: "numeric"
  }).format(new Date(dateString));
}

export default function No23VideoLibrary() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("Tümü");

  const categories = ["Tümü", ...Array.from(new Set(matches.map((m) => m.category)))];

  const filteredMatches = useMemo(() => {
    return matches.filter((match) => {
      const text = `${match.home} ${match.away} ${match.category} ${match.league} ${match.venue} ${match.note}`.toLowerCase();
      const matchesQuery = text.includes(query.toLowerCase());
      const matchesCategory = category === "Tümü" || match.category === category;
      return matchesQuery && matchesCategory;
    });
  }, [query, category]);

  return (
    <div className="min-h-screen bg-slate-950 text-white">
      <section className="relative overflow-hidden border-b border-white/10">
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_top_left,rgba(220,38,38,0.32),transparent_35%),radial-gradient(circle_at_bottom_right,rgba(255,255,255,0.12),transparent_30%)]" />
        <div className="relative mx-auto max-w-7xl px-5 py-12 md:py-16">
          <div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 text-sm text-white/80 backdrop-blur">
            <Lock className="h-4 w-4" />
            Sadece linki bilen aileler ve koçlar için özel maç arşivi
          </div>

          <div className="grid gap-8 md:grid-cols-[1.4fr_0.8fr] md:items-end">
            <div>
              <h1 className="max-w-3xl text-4xl font-bold tracking-tight md:text-6xl">
                No23 Basketball Academy Maç Video Kütüphanesi
              </h1>
              <p className="mt-5 max-w-2xl text-lg leading-8 text-white/70">
                YouTube liste dışı maç videoları, istatistik dosyaları ve maç notları tek yerde. Arama ve filtreleme ile istenen maça saniyeler içinde ulaşılır.
              </p>
            </div>

            <Card className="rounded-3xl border-white/10 bg-white/10 text-white shadow-2xl backdrop-blur">
              <CardContent className="p-6">
                <div className="grid grid-cols-3 gap-4 text-center">
                  <div>
                    <div className="text-3xl font-bold">{matches.length}</div>
                    <div className="mt-1 text-xs text-white/60">Maç</div>
                  </div>
                  <div>
                    <div className="text-3xl font-bold">3</div>
                    <div className="mt-1 text-xs text-white/60">Kategori</div>
                  </div>
                  <div>
                    <div className="text-3xl font-bold">100%</div>
                    <div className="mt-1 text-xs text-white/60">Linkli</div>
                  </div>
                </div>
              </CardContent>
            </Card>
          </div>
        </div>
      </section>

      <main className="mx-auto max-w-7xl px-5 py-8">
        <Card className="mb-8 rounded-3xl border-white/10 bg-white/[0.06] text-white shadow-xl">
          <CardContent className="p-5">
            <div className="grid gap-4 md:grid-cols-[1fr_auto]">
              <div className="relative">
                <Search className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-white/40" />
                <input
                  value={query}
                  onChange={(e) => setQuery(e.target.value)}
                  placeholder="Rakip, lig, salon, kategori veya not ara..."
                  className="w-full rounded-2xl border border-white/10 bg-white/10 py-4 pl-12 pr-4 text-white outline-none placeholder:text-white/40 focus:border-white/30"
                />
              </div>

              <div className="flex items-center gap-2 overflow-x-auto">
                <Filter className="hidden h-5 w-5 text-white/50 md:block" />
                {categories.map((item) => (
                  <Button
                    key={item}
                    onClick={() => setCategory(item)}
                    variant="ghost"
                    className={`rounded-2xl px-5 py-6 ${
                      category === item ? "bg-white text-slate-950 hover:bg-white" : "bg-white/10 text-white hover:bg-white/20"
                    }`}
                  >
                    {item}
                  </Button>
                ))}
              </div>
            </div>
          </CardContent>
        </Card>

        <div className="mb-4 text-sm text-white/50">
          {filteredMatches.length} maç listeleniyor
        </div>

        <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">
          {filteredMatches.map((match) => (
            <Card key={match.id} className="group overflow-hidden rounded-3xl border-white/10 bg-white/[0.06] text-white shadow-xl transition hover:-translate-y-1 hover:bg-white/[0.09]">
              <CardContent className="p-0">
                <div className="relative aspect-video bg-gradient-to-br from-red-700 via-slate-900 to-slate-950 p-6">
                  <div className="absolute right-4 top-4 rounded-full bg-white px-3 py-1 text-xs font-bold text-slate-950">
                    {match.category}
                  </div>
                  <div className="flex h-full flex-col justify-between">
                    <Trophy className="h-10 w-10 text-white/80" />
                    <div>
                      <div className="text-sm text-white/60">{match.league}</div>
                      <h2 className="mt-2 text-2xl font-bold leading-tight">
                        {match.home} vs {match.away}
                      </h2>
                    </div>
                  </div>
                </div>

                <div className="p-6">
                  <div className="mb-5 space-y-3 text-sm text-white/70">
                    <div className="flex items-center gap-2">
                      <CalendarDays className="h-4 w-4" />
                      {formatDate(match.date)}
                    </div>
                    <div className="flex items-center gap-2">
                      <Users className="h-4 w-4" />
                      {match.venue}
                    </div>
                  </div>

                  <div className="mb-5 flex flex-wrap gap-2">
                    {match.tags.map((tag) => (
                      <span key={tag} className="rounded-full bg-white/10 px-3 py-1 text-xs text-white/70">
                        {tag}
                      </span>
                    ))}
                  </div>

                  <p className="mb-6 min-h-[44px] text-sm leading-6 text-white/60">{match.note}</p>

                  <div className="grid grid-cols-2 gap-3">
                    <a href={match.videoUrl} target="_blank" rel="noreferrer">
                      <Button className="w-full rounded-2xl bg-red-600 py-6 text-white hover:bg-red-700">
                        <PlayCircle className="mr-2 h-5 w-5" />
                        Video
                      </Button>
                    </a>
                    <a href={match.statsUrl} target="_blank" rel="noreferrer">
                      <Button className="w-full rounded-2xl bg-white py-6 text-slate-950 hover:bg-white/90">
                        <BarChart3 className="mr-2 h-5 w-5" />
                        İstatistik
                      </Button>
                    </a>
                  </div>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>

        <section className="mt-12 rounded-3xl border border-white/10 bg-white/[0.06] p-6 text-white/70">
          <div className="mb-3 flex items-center gap-2 text-white">
            <ExternalLink className="h-5 w-5" />
            Yönetim mantığı
          </div>
          <p className="leading-7">
            Her yeni maç için sadece maç adı, tarih, kategori, YouTube liste dışı linki ve istatistik linki eklenir. Site otomatik olarak arşiv gibi çalışır. İleride giriş şifresi, oyuncu filtresi, highlight etiketi ve koç notu alanı da eklenebilir.
          </p>
        </section>
      </main>
    </div>
  );
}
